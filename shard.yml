name: elephante
version: 0.1.0

authors:
  - name <russ@bashme.org>

description: |
  A postgres based job system.

dependencies:
  pg:
    github: will/crystal-pg
    version: "~> 0.5"

license: MIT
